{
  "name": "@solncebro/market-data-feeder",
  "version": "0.1.0",
  "description": "On-demand market-data feeder: one exchange connection, fans klines/MA/24h-volume out to multiple strategy apps over a local WebSocket channel",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "yarn lint && yarn test && rm -rf dist && tsc",
    "start": "node dist/feeder.js",
    "dev": "tsx src/feeder.ts",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy:binance:leo": "yarn build && ssh root@209.38.110.21 'mkdir -p /opt/market-data-feeder-binance/' && rsync --archive --compress --delete --copy-unsafe-links .env.binance.leo dist package.json yarn.lock node_modules root@209.38.110.21:/opt/market-data-feeder-binance/ && ssh root@209.38.110.21 'cd /opt/market-data-feeder-binance && mv .env.binance.leo .env'",
    "deploy:binance": "yarn build && ssh root@82.26.198.214 'mkdir -p /opt/market-data-feeder-binance/' && rsync --archive --compress --delete .env.binance dist package.json yarn.lock node_modules root@82.26.198.214:/opt/market-data-feeder-binance/ && ssh root@82.26.198.214 'cd /opt/market-data-feeder-binance && mv .env.binance .env'",
    "deploy": "yarn build && ssh root@82.26.198.214 'mkdir -p /opt/market-data-feeder-bybit/' && rsync --archive --compress --delete .env.bybit dist package.json yarn.lock node_modules root@82.26.198.214:/opt/market-data-feeder-bybit/ && ssh root@82.26.198.214 'cd /opt/market-data-feeder-bybit && mv .env.bybit .env'",
    "deploy:all": "yarn deploy && yarn deploy:binance && yarn deploy:binance:leo"
  },
  "dependencies": {
    "@solncebro/telegram-engine": "file:../telegram-engine",
    "@solncebro/trade-engine": "file:../trade-engine",
    "@solncebro/websocket-engine": "file:../websocket-engine",
    "dotenv": "^16.4.5",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
